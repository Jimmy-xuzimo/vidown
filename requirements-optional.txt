# Vidown —— 可选依赖
# 这些包提供增强功能，按需安装。

# 浏览器 Cookie 导入（解锁会员/年龄限制内容）
browser-cookie3>=0.19.0

# you-get 备用引擎（主要支持中文站点）
you-get>=0.4.1655

# 异步 HTTP（性能更好的并发下载）
aiohttp>=3.9.0
httpx>=0.27.0

# 页面渲染（需要 JavaScript 提取的页面）
playwright>=1.42.0
selenium>=4.18.0
